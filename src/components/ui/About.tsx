import React from "react";
import { SectionId } from "../../constants/sections";

const About = () => {
  return (
    <section id={SectionId.About} className="min-h-screen bg-purple-400">
      <div>About</div>
    </section>
  );
};

export default About;
