import React from 'react'
import { SectionId } from '../../constants/sections'

const Hero = () => {
  return (
    <section id={SectionId.Hero} className='min-h-screen bg-purple-500'>
      <div>hero</div>
    </section>
  )
}

export default Hero
